# 需要 Python >= 3.12（polymarket-apis 官方要求；Debian 12 见 doc/DEBIAN12_PYTHON312.md）
# 若 pip 报「Could not find polymarket-apis / from versions: none」：当前解释器 <3.12，请用
# python3.12 -m venv .venv 重建环境后再 pip install -r requirements.txt
aiohttp>=3.9.0
websockets>=12.0
python-dotenv>=1.0.0
py-clob-client>=0.18.0
web3>=6.0.0
eth-account>=0.10.0
apscheduler>=3.10.0
requests>=2.31.0
PySocks>=1.7.1
polymarket-apis>=0.5.2   # 自动兑换（官方SDK无此功能，Issue #139）
